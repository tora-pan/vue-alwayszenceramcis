// STATE
const state = {
  cartItems: [],
};

// MUTATIONS
const mutations = {};
// ACTIONS (asynchronous) ({commit})
const actions = {};
// GETTERS
const getters = {};
const cartModule = {
  state,
  mutations,
  actions,
  getters,
};

export default cartModule;
