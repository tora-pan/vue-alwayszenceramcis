// STATE
const state = {
  shopItems: [],
};

// MUTATIONS
const mutations = {};
// ACTIONS (asynchronous) ({commit})
const actions = {};
// GETTERS
const getters = {};
const shopModule = {
  state,
  mutations,
  actions,
  getters,
};

export default shopModule;
